#include <iostream> //files useful for input and output to console
#include <string>
#include <vector>
#include <array>
#include <stdio.h>
#include <string>

namespace Arron
{
    void printName(const string name)
    {
        cout << name << endl;
    }
}
